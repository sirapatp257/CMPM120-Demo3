let gameConfig = {
   scale: {
      mode: Phaser.Scale.FIT,
      autoCenter: Phaser.Scale.CENTER_BOTH,
      width: 1920,
      height: 1080
   },
   scene: [],
   title: "CMPM 120 - D3"
};

const game = new Phaser.Game(gameConfig);